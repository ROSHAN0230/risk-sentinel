services:
  - type: web
    name: risk-sentinel
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "uvicorn src.engine.api:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: "/v1/health"
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
